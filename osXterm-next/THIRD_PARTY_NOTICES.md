# Third-party notices

This application declares a dependency on SwiftTerm 1.19.0. SwiftTerm is distributed under the MIT License. The packaging script copies the dependency resource bundles that Swift Package Manager produces; the final release process must also copy the upstream license text from the resolved dependency into the app bundle.

OpenSSH is supplied by macOS and is not bundled with osXterm. Its license and notices remain those distributed by Apple with the operating system.
